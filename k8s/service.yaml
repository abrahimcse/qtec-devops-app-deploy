apiVersion: v1
kind: Service
metadata:
  name: qtec-service
spec:
  selector:
    app: qtec-devops-task
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001
